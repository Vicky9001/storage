spring:
  #nacos配置
  application:
    name: warehousing-management
  cloud:
    nacos:
      config:
        # 配置Nacos注册中心地址
        #        server-addr: 39.107.156.214:8848
        server-addr: localhost:8848